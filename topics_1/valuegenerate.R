# Value Generation using these functions

#seq
seq(1,100, by=5)
seq(0,99, length.out=10)

#rep
rep(c(1,2,3), times=5)
rep(c(1,2,3), each=5)

#rnorm
rnorm(n=100, mean=60, sd=10)


#runif
runif(n=100, min=60, max=100)

#sample
sample(1:10, size=100, replace=T)

#months
month.abb[1:10]
#alphabets
letters[1:10]


#Built in Constants
LETTERS
letters
month.abb
month.name
pi
